# ── API ──
# Base URL of the ChowZilla backend.
VITE_API_URL=http://localhost:5000

# ── Payments (Paystack) ──
# Paystack *public* key (safe to expose in the browser; starts with pk_).
VITE_PAYSTACK_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxx
